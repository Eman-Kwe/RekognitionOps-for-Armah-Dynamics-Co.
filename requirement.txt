# Python packages this project needs
# GitHub Actions runs pip install -r requirements.txt before every workflow


boto3>=1.26.0                                   # AWS SDK — connects Python to S3, Rekognition, DynamoDB
python-dotenv>=0.19.0                           # Loads .env file for local development testing